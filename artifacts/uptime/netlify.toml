[build]
  base    = "artifacts/uptime"
  command = "pnpm install && pnpm run build"
  publish = "dist/public"

[build.environment]
  NODE_VERSION  = "20"
  PNPM_VERSION  = "9"

[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
