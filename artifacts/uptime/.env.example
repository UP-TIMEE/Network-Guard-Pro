# Optional: Set this to your deployed API server URL when hosting on Netlify/Vercel
# Leave empty when running on Replit (API is served from the same origin)
# VITE_API_BASE_URL=https://your-api-server.onrender.com
